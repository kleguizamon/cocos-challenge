version: '3.8'

services:
  api:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: cocos-challenge
    env_file:
      - .env
    ports:
      - "${PORT:-4000}:${PORT:-4000}"
    restart: unless-stopped